spring:
  application:
    name: userservice

security:
  internal:
    secret: ${SERVICES_INTERNAL_SECRET:internal-secret}

gateway:
  internal-signing-secret: ${GATEWAY_INTERNAL_SIGNING_SECRET:gateway-internal-secret}
